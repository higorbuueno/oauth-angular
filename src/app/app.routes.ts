import { Routes } from '@angular/router';
import { LoginComponent } from './screens/auth/login.component';

export const routes: Routes = [
    {
        path: '',
        component: LoginComponent
    }
];
